namespace ChargeSlot.Api.Enums
{
    /// <summary>Charging slot status (SRS UC-30, UC-31).</summary>
    public enum SlotStatus
    {
        Active = 0,
        Inactive = 1,
        Maintenance = 2
    }
}
